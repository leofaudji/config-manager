# Use the official Falcosidekick image as the base
FROM falcosecurity/falcosidekick:2.28.0

# Copy the configuration file that will be generated by the PHP script
# into the container's filesystem at build time.
COPY config.yaml /config.yaml